<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        body {
      font-family: Arial, sans-serif;
      background-image:  linear-gradient(to right,#9187f0, #c0e4f3);
    }
    .container {
      max-width: 800px; /* Increase container width */
      margin: 20px auto; /* Add margin around container */
      padding: 20px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      text-align: center;
    }
    h1 {
      font-size: 36px; /* Increase heading font size */
      margin-top: 0;
      margin-bottom: 20px; /* Add margin below heading */
    }
    .company-card {
      max-width: 600px; /* Increase company card width */
      margin: 0 auto; /* Center company card */
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      padding: 20px;
      text-align: center;
    }
    .company-info {
      font-size: 18px;
      margin-bottom: 20px;
    }
    .withdraw-button {
      background-color: #ff5757;
      color: #fff;
      border: none;
      padding: 10px 20px;
      font-size: 16px;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }
    .withdraw-button:hover {
      background-color: #e74c3c;
}   
    #myid{
        color: aqua;
    }
</style>
    
</head>
<body>
    <div class="container">
    <h1>Company Dashboard</h1>
</div>
    <div class="company-card">
    <h2>Hello, </h2>
    <h1 id="myid"><%= name %></h1>
    <div class="company-info">
        <p>Total funding required: <%= funding %> ETH</p>
        <p>Funding received: <%= fr %> ETH</p>
        <p><b> <%= message %></b></p>
         </div> 

    <div id="withdraw" name="withdraw">
        <form method="post" action="/withdraw">
        <button class="withdraw-button">WITHDRAW</button>
    </form>
</div>
    </div>
    <a href="/">Logout</a>
</body>
</html>